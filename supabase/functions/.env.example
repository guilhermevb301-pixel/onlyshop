# Chaves usadas pelas edge functions do OnlyShop.
# COPIE este arquivo para `.env` (que é gitignorado) e preencha a sua chave.
#
#   cp supabase/functions/.env.example supabase/functions/.env
#
# Depois, pra usar em produção, suba como secret no Supabase:
#   supabase secrets set FAL_KEY=sua_chave_aqui

# fal.ai — geração de vídeo (Seedance/Kling/Veo). Pegue em https://fal.ai/dashboard/keys
FAL_KEY=

# OpenAI — gera a copy/roteiros (ai-copilot + generate-video). Pegue em https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Opcional: modelo do GPT. Default = gpt-4o. Ex: gpt-4o-mini (mais barato).
OPENAI_MODEL=gpt-4o

# Apify — indicadores do TikTok Creative Center (produtos/hashtags/criativos em alta).
# Usado pela função tiktok-trends. Pegue em https://console.apify.com/account/integrations
APIFY_TOKEN=

# Opcional: slug do actor do Apify (default: doliz~tiktok-creative-center-scraper)
APIFY_ACTOR=doliz~tiktok-creative-center-scraper
